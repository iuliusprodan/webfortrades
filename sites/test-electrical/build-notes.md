# Build notes — Hartley Electrical (test)

- Slug: `test-electrical`
- Direction: quiet-premium-editorial (Syne + DM Sans, amber accent)
- Test build — placeholder image blocks, no real photos
- Static export ready for Vercel

## Run locally

```bash
cd sites/test-electrical
npm run dev
```

Open http://localhost:3000
